{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/style.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add</title>
</head>
<body>
    <div class="container">
        <h1>Изменение контакта</h1>
        {{> contactList}}
        <form id="updateForm" method="POST">
            <input type="hidden" name="id"  value="{{selectedUserId}}">
            <input name="name" type="text" class="input" value={{selectedUserName}}>
            <input name="number" type="text" class="input" value={{selectedUserNumber}}>
            <div class="buttonContainer">
                <button type="submit" class="UpdateButton">Изменить</button>
                <button type="button" class="DelButton"  onclick="submitDeleteForm()">Удалить</button>
            </div>
        </form>
        <div class="buttonContainer">
            {{{cancelButton cancelButtonUrl cancelButtonText}}}
        </div>
    </div>
    <script src="./disable.js" type="text/javascript"></script>
    <script src="./blockDeleteButton.js" type="text/javascript"></script>

</body>
</html> --}}

 <div class="container">
        <h1>Изменение контакта</h1>
        <form id="updateForm" method="POST">
            <input type="hidden" name="id"  value="{{selectedUserId}}">
            <input name="name" type="text" class="input" value={{selectedUserName}}>
            <input name="number" type="text" class="input" value={{selectedUserNumber}}>
            <div class="buttonContainer">
                <button type="submit" class="UpdateButton">Изменить</button>
                <button type="button" class="DelButton"  onclick="submitDeleteForm()">Удалить</button>
            </div>
        </form>
        <div class="buttonContainer">
            {{{cancelButton cancelButtonUrl cancelButtonText}}}
        </div>
    </div>
    <script src="./disable.js" type="text/javascript"></script>
    <script src="./blockDeleteButton.js" type="text/javascript"></script>
